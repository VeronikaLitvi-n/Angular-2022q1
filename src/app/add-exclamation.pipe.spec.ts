import { AddExclamationPipe } from './add-exclamation.pipe';

describe('AddExclamationPipe', () => {
  it('create an instance', () => {
    const pipe = new AddExclamationPipe();
    expect(pipe).toBeTruthy();
  });
});
